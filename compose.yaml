services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: dash
      POSTGRES_PASSWORD: dashpass
      POSTGRES_DB: friends_dash
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data